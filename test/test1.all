    include """test2.all"""
    
  

    struct person {
        i8 age
        i8 * name
    }


    extern func i8 main(){
        i8 a = 8    /*
        
        
        the king
        
        */
        i8 b = 9
        i8 c = a + b
        i8 d = a * b * (c + a + b)
        float x = 86.002
        person ** person1 = {16,@addr("""Eymen""")}
        array<i8,3> arr = {2,3,8}
        if (b == 8) {}
        else {}
        do while (0) {}
        while (0) {}
        print(person1.name,5)
        return 0 
    }

    func void print(i8 * string, i16 len){
        ASM {
            mov %rax,%rsi
            mov %rsi,%rdx
            mov %rdi,1
            mov %rdx,%rax
            mov %rax,1
            syscall
        }
        return
    }
